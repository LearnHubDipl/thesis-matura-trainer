@startuml
title Activity Diagramm – Vereinfachter User Flow

start

:Login;

repeat
  :Dashboard anzeigen;
  :Modus auswählen;

  if (Üben) then (ja)
    :Fragen auswählen;
    :Fragen beantworten;
    :Feedback erhalten;
    :Ergebnis speichern;
  else (nein)
    if (Prüfung) then (ja)
      :Prüfung starten;
      :Fragen beantworten;
      :Prigfung abschließen;
      :Auswertung erhalten;
      :Ergebnis speichern;
    else (nein)
      :Statistiken anzeigen;
    endif
  endif

  backward: (Zurück zum Dashboard);
repeat while (Logout?) is (nein)
->ja;

stop
@enduml
